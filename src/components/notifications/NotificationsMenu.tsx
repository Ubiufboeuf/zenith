export function NotificationsMenu ({ class: className = '' }: { class?: string }) {
  return (
    <section class={`${className} absolute right-0 top-10 w-40 h-60 rounded-lg border border-neutral-700 bg-base-300`}>
      
    </section>
  )
}
